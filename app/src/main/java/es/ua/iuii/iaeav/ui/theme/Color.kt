package es.ua.iuii.iaeav.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * # Definición de Colores de la Paleta
 *
 * Este archivo contiene las constantes de color principales (basadas en la paleta del logo IAEAV)
 * utilizadas para construir el tema claro y el tema oscuro de Material 3.
 */

// --- Colores para el TEMA CLARO ---

/** Azul Primario: Color sólido base para elementos principales de la UI (ej. botones). */
val PrimaryBlue = Color(0xFF009ACD)       // Un azul primario sólido

/** Azul Secundario: Color para elementos complementarios o de énfasis en el tema claro. */
val SecondaryBlue = Color(0xFF007B9A)      // Un azul más oscuro para elementos secundarios

/** Aqua Terciario: El toque de "aqua" o cian brillante, usado para destacar o acentuar. */
val TertiaryAqua = Color(0xFF00E5FF)       // El toque de "aqua" brillante del logo

// --- Colores para el TEMA OSCURO ---

/** Azul Primario Claro: Tono azul claro y legible para el modo oscuro (sustituye a PrimaryBlue). */
val PrimaryBlueLight = Color(0xFF73CFFF)    // Un azul claro y legible para el modo oscuro

/** Azul Secundario Claro: Tono complementario para elementos secundarios en el modo oscuro. */
val SecondaryBlueLight = Color(0xFF4FC3F7)  // Un tono complementario

/** Aqua Terciario Claro: El toque "aqua" pálido, usado como acento en el modo oscuro. */
val TertiaryAquaLight = Color(0xFFB3EFFF)   // El toque "aqua" pálido